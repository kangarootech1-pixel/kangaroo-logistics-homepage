VITE_FORMS_WEBHOOK_URL=
VITE_CHAT_WEBHOOK_URL=

# Server-side only (used by api/jobs.ts). NEVER prefix with VITE_ — that would
# inline the secret into the public client bundle. Set these as environment
# variables in the Vercel dashboard for production.
NOTION_TOKEN=
NOTION_DATABASE_ID=
